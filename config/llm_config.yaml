provider: openai

model: gpt-4
temperature: 0.1
max_tokens: 2000
api_key_env: OPENAI_API_KEY

local_model:
  enabled: false
  model_path: models/local_llm
  device: cuda

embeddings:
  model: text-embedding-3-small
  dimensions: 1536

agent_type: react
max_iterations: 10
verbose: false
